var unpack = require("unpack-element")
    , Fragment = require("fragment")

module.exports = html

function html(source) {
    var elem = Fragment(source.trim())

    if (elem.childNodes.length === 1) {
        elem = elem.firstChild
    }

    return unpack(elem)
}
